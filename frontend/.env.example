# URL of the deployed backend. For local dev, the default in src/api.ts
# (http://localhost:8000) already works, so this file is only needed once
# you deploy the frontend separately from the backend.
VITE_API_BASE_URL=http://localhost:8000
